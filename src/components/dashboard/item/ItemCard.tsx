import type { ItemData } from './Item'

const ItemCard = ({ 
  id,
  title,
  description,
  sold, 
  seller, 
  itemVariant, 
  itemImage, 
  _count 
}: ItemData) => {
  return (
    <div>
    </div>
  );
}

export default ItemCard;